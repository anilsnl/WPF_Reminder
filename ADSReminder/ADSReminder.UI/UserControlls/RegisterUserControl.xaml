<UserControl x:Class="ADSReminder.UI.UserControlls.RegisterUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADSReminder.UI.UserControlls"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="500" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Register"  Style="{DynamicResource lblTitle1}"/>
        <ScrollViewer VerticalScrollBarVisibility="Visible" CanContentScroll="True"  Grid.Row="1"  >

            <StackPanel Margin="15" HorizontalAlignment="Center" Background="White" 
                    CanVerticallyScroll="True"
                    CanHorizontallyScroll="True">
                <Label  Content="Name Surname*"
                            Style="{DynamicResource lblT1}"/>
                <TextBox Style="{DynamicResource txtForm}"
                         Text="{Binding Path=User.NameSurname,Mode=TwoWay}"/>

                <Label  Content="Username"
                            Style="{DynamicResource lblT1}"/>
                <TextBox Style="{DynamicResource txtForm}"
                         Text="{Binding Path=User.Username,Mode=TwoWay}"/>

                <Label  Content="New Password*" 
                        Style="{DynamicResource lblT1}"/>
                <PasswordBox Style="{DynamicResource passFrm}" 
                             x:Name="pswPassword"
                             PasswordChanged="pswPassword_PasswordChanged"
                             PasswordChar="*" />
                <Label  Content="Confirm Password*" 
                Style="{DynamicResource lblT1}"/>
                <PasswordBox Style="{DynamicResource passFrm}" 
                             x:Name="pswConfirm"
                             PasswordChanged="pswConfirm_PasswordChanged"
                             PasswordChar="*" />

                <Label  Content="Secret Question*"
                            Style="{DynamicResource lblT1}"/>
                <TextBox Style="{DynamicResource txtForm}"
                         Text="{Binding Path=User.SecretQuestion,Mode=TwoWay}"/>

                <Label  Content="Secret Answer*"
                            Style="{DynamicResource lblT1}"/>
                <TextBox Style="{DynamicResource txtForm}"
                         Text="{Binding Path=User.SecretAnswer,Mode=TwoWay}"/>

                <Button Content="Register And Login" HorizontalAlignment="Right" 
                        Style="{DynamicResource btnDanger}" Width="250"
                        Command="{Binding RegisterCommand}"/>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
